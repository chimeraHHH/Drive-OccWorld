# O 完整共同验证：独立结果审查

2026-09-15。**独立审查通过：固定 O 在完整原生 GMO 未来预测指标上超过 M0_fp32 和同预算 C，实质效应在 full 验证中保持。**没有读取运行中性能；本次在真实 EXITED_ZERO 后，仅使用本地镜像重新检查来源与数学，没有服务器任务、模型推理、训练、skills 或冻结源修改。

## 独立重算的结果

下表单位为 GMO IoU %；未来平均是各未来时域分别池化完整 GT 混淆后计算 IoU，再平均四个时域。

| 模型 | t0 | +0.5 s | +1.0 s | +1.5 s | +2.0 s | 未来平均 |
|---|---:|---:|---:|---:|---:|---:|
| M0 | 15.97238 | 15.18346 | 14.36849 | 13.52312 | 12.70243 | 13.94438 |
| M0_fp32 | 15.97239 | 15.18357 | 14.36862 | 13.52366 | 12.70356 | 13.94485 |
| C / native1 | 15.74468 | 14.84091 | 14.12876 | 13.37036 | 12.57056 | 13.72765 |
| O | 15.83281 | 15.63715 | 15.15352 | 14.48657 | 13.71393 | 14.74779 |

| 未来平均比较 | 差值 pp | 95% 场景配对区间 pp |
|---|---:|---:|
| O − M0_fp32（主比较） | +0.802942 | [+0.681782, +0.940610] |
| O − C（主比较） | +1.020147 | [+0.929598, +1.111506] |
| C − M0_fp32 | −0.217205 | [−0.326359, −0.106035] |
| M0_fp32 − M0（数值条件对照） | +0.000475 | [−0.000350, +0.001352] |

直接由点估计相减，O−原生 M0 为 **+0.803417 pp**；这是原参考的补充比较，不替代两个冻结主比较。精度差没有解释 O 的收益。两个主比较均达到既定 0.5 pp 实质参照且区间下界为正；不需新增或改变成功门槛。

**t0 代价真实存在且区间已计算：**O−M0_fp32 为 −0.139579 pp，区间 [−0.168239, −0.109437]；O−C 则为 +0.088137 pp，区间 [+0.069460, +0.108051]。这些区间由本次原始数组独立重算，不能沿用旧绘图说明中的“未计算 t0 CI”。O 对 M0 的四个未来时域均提高，但不意味着所有时域都提高。

错误计数也须区分比较：O−M0_fp32 的四未来时域 pooled FP 为 −26,205,756、FN 为 −321,957；其中 +1.5/+2.0 s 的 FN 实际增加。O−C 的 pooled FP 为 −96,713,587、FN 为 +8,281,704。因此“各类错误或每时域 recall 均改善”不成立，O 相对 C 的收益包含降低误报与增加漏报的取舍。

## 本次实际核验了什么

- **数据及共同 GT：**直接读取 `raw_confusions.npz` 的整数数组 `[4,5119,5,2,2]`，核四模型顺序、全部唯一 token/official index、五时域、selection 与 ledger 身份。所有模型逐 anchor、逐时域混淆的 GT 行计数均精确等于共同七帧 GT 计数中的当前及四未来帧；每帧 0/1/255 合计均为 `512×512×40`。150 场景自然样本量为 18×33、95×34、37×35，未改为每场景两条。
- **独立数学：**没有调用 producer、merger、aggregate 或 plot 的数学 helper。用独立 int64 代码按场景累加混淆，生成 `RandomState(11)` 的 10,000 次 multinomial 场景抽样，整场景保留全部 anchors；直接以整数矩阵乘法生成重采样计数，然后计算 IoU、binary mIoU、未来 macro/pooled 指标和配对差值区间。四模型全部五时域及四组比较共 296 项 scalar 与正式 summary 精确一致，抽样矩阵 SHA 也一致。
- **完整来源链：**重核 summary/complete 及全部七项输出文件 SHA；重核冻结 parent、objective、full 协议和授权、selection；本地重核 43 份绑定 Python/原生 runtime 源。补齐的 contract、两 shard manifest/index/loaded_models 共七份元数据全部通过。两分片 2560/2559 条索引与合并 ledger 的 5119 条身份、records/complete 哈希逐条一致，无缺失或重复；共同 contract、数值条件、原生雷达路径、M0 epoch24 和 C/O 最终 checkpoint/head/migration 均与冻结记录一致。
- **额外历史交叉核对：**对已认证的 C、M0_fp32、O 开发集旧记录，各200条按 token 与此次 full 原始混淆对齐，三组全部五时域精确相同。这是600条历史混淆复现，不是声称全5119条历史 cache 的全部输入叶均重新比较。
- **执行回执：**两 shard 完成且 exit code=0，optimizer=0、无阈值选择、规划关闭。外层 runner 实际驻留 `8528.042677880032 s`，依据 `receipts/20260915_064516.json`；内部 wrapper 为 `8527.822559379973 s`。每 shard 硬上限21600 s、32 GiB；最大实测 allocated 为5,373,955,584字节。不能把两并行 shard 驻留相加当作用户等待时间。

本地源路径映射是 `/home/wangning/Workspace/RadarFlowOcc-sota-p2/...` → `code/Drive-OccWorld-sota-p2/...`，仅在实际 SHA 相同时接受。本次未重读大 checkpoint、原始图像/雷达/逐 voxel GT，也没有下载并重哈希5119份单样本 records。对应的原始资产和 GPU 加载身份由已冻结 producer 的回执链支持；本地重新证实的是完整统计、索引、来源承诺及可用元数据，不冒称新的神经网络复现。

## 结论范围

证据支持 O 这个真实训练后的固定候选在原任务、完整共同 validation 和既定两个控制下获得实质提升；后验 C/O 2×2 是否阳性不能改变这一事实或门槛。当前 val 有历史曝光，完整集也包含开发场景；这不是新盲测。区间未调整多重比较，只覆盖固定模型的场景抽样不确定性，不覆盖单训练 seed11 的稳定性或既往模型选择。已有 loss 删减的有效性不等于 ICLR 新颖性，训练风险、尺度/裁剪及表征/读出的因果机制仍待独立研究。

关键哈希：summary `199347570b9d34130c6366ef3fb138bbc08c1310a50010a1f200061fe428f463`；complete `cdf54175b84613b1e175fe783f490ab62681c18060c5289981f2be426d2a53e5`；raw NPZ `4f649ede18d41e371bb36837e642b99e095c24936601761e1f402744399366f4`；ledger `cc5fde3022bd5450ec86498e3889d6a7c120c1da1066514794859bacc09eefac`；contract `bca9304065d996bfc80dbb228521ea62bef7e55605750dc88c4e971e5c0befe0`；bootstrap draws `8b8f942caa00bda54047a9207c74f88a6291eb520aa4b61214b74dfd141b725a`。
