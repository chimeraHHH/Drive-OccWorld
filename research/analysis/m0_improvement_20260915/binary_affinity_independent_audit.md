# 二分类 scaling 与真实记录的独立审查

2026-09-15。结论：**未发现推翻当前数学或真实数值结论的问题**。本次只读源码及已完成记录，在本机 CPU 独立复算；没有模型调用、梯度、GPU/server 操作、mock 或 partial full 性能读取。

1. **标签解释正确。**已重核 S0、loader 和 loss 源码 SHA。`loading_occupancy.py:98–110` 的类合并使 class0 同时含原未占据和静态背景，class1 是 GMO 语义集合，255 排除。`semkitti_loss.py:65–89` 实际把合并后的 `target!=0` 当正类；不能将 geo 项理解成额外物理自由空间或运动真值。
2. **理想公式及 ε 正确。**两类都出现、概率精确互补且处于普通可微范围时，sem 两类平均确实给出 `0.5ℓ(P)+0.5ℓ(N)+ℓ(R)+ℓ(S)`；geo 增加 `ℓ(P)+ℓ(R)+ℓ(S)` 及三个 `log1p(ε/denominator)`。因此总系数为 `1.5/0.5/2/2`。GT 正负计数对应两项 ε 修正对预测参数为常数，预测质量那项不是。缺类分支、FP32 归约、`p1` 与 `1−p0` 的差异均阻止将此恒等式直接推广成原 CUDA 梯度等价。
3. **BCE 边界描述正确。**独立打开 PyTorch v2.1.2 [CUDA Loss.cu](https://raw.githubusercontent.com/pytorch/pytorch/v2.1.2/aten/src/ATen/native/cuda/Loss.cu)：27 行定义 1e−12；47–52 行限制反向分母；87–95 行检查概率范围并截断前向 logarithm。该 backward 不能被简单当成截断前向函数处处的数学导数。现有运行依赖版本证据为 `receipts/20260915_005002.json:104` 的 `2.1.2+cu121`；本次未重新调用服务器。192 行保存比值最小 `4.403444885893152e−7`、最大 `0.9999898526389548`，最小 `p(1−p)=4.403442946860466e−7`，没有接近这些数值分支；这仍是序列化统计判断，不是实际 CUDA 分支重放。
4. **真实复算通过。**独立重核 source summary/manifest/complete、四模型 JSONL 和输出 summary/CSV 的哈希链，192 行三项残差逐行精确重算一致。sem、geo 近似 scalar 与原 loss 最大绝对差分别为 `1.0682542086470903e−6`、`2.6282742737748777e−6`；理想合并代入式加 ε 的最大残差 `0.036072174538624324`，对应 M0_fp32、token `706d7015b80d4329b80d4a8c52139ab4`、layer1。记录仅为四个旧模型×16个训练 anchors×3层的当前加未来池化统计，既不是192个独立样本，也不含 F/O。

两个非阻断边界已反馈根任务：其一，字段 `ideal_binary_combination` 是用各通道已舍入统计代入理想公式，宜称“理想合并公式的代入残差”，并非由单一精确互补概率重新构建的 CUDA loss。其二，脚本可将 `complete.records_sha256`、`summary.records_sha256` 显式互核，使固定 summary 的来源约束直接传递到记录；本次独立核验已确认实际两份 ledger 完全相同，现有记录与结果没有因此失效。

证据支持“在此二分类定义下两项重用部分统计、有效权重需要解释”，不支持损失相互冲突、删除项必然更优、舍入残差导致训练退化，或 O 的收益已由某一因素解释。没有建议更改 full 成功标准或启动新训练。

审查版本：脚本 SHA `685bc584b291825336219a5cad360e7d6c95d6d2c032c72c68f731efbd81a441`；分析文 SHA `ff85176a7e99a5e8a7634d6c3e68ba62b8cb6f81d10ae737d7ecd9381640b74f`；结果 summary SHA `73e800e9d3b29cfd86b5c98d898918e0aa11e9167deb86267c3387784005271a`；CSV SHA `dcc9f3b713400f13013dbbb31582d324b5cebdc43523cf2ef7608a0089fa603c`；complete SHA `f6cbee15c6124e7e14eb4d524579457ad6e012b44386046a78584408689f2a9f`。
